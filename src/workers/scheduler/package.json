{
  "name": "scheduler-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for scheduled job initiation and N8N workflow triggering",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0",
    "vitest": "^1.0.0"
  }
}