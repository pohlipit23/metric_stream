{
  "name": "admin-console-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Admin Console API endpoints",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest --run"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240925.0"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "wrangler": "^3.0.0"
  }
}